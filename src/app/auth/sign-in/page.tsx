import LoginForm from './components/login-form';

const LoginPage = () => {
    return (
        <div className="flex w-screen items-center justify-center h-svh flex-col">
            <LoginForm />
        </div>
    );
};

export default LoginPage;
