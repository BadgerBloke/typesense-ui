import Typography from '../atoms/typography';

const Logo = () => {
    return (
        <Typography variant="large">
            Typesense <span className="gradient-bg px-1 py-0.5 rounded-md">UI</span>
        </Typography>
    );
};

export default Logo;
